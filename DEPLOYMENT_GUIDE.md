# 🚀 Гайд по развертыванию BlinkSite в продакшн

Этот гайд поможет вам купить домен, развернуть фронтенд и бэкенд, чтобы сайт работал не локально.

---

## 📋 Содержание

1. [Покупка домена](#1-покупка-домена)
2. [Деплой фронтенда на Vercel](#2-деплой-фронтенда-на-vercel)
3. [Деплой бэкенда на Railway](#3-деплой-бэкенда-на-railway)
4. [Подключение домена](#4-подключение-домена)
5. [Финальная настройка](#5-финальная-настройка)

---

## 1. 🌐 Покупка домена

### Где купить домен

Есть несколько популярных регистраторов:

| Регистратор | Плюсы | Минусы | Цены |
|-------------|-------|--------|------|
| **[Namecheap](https://namecheap.com)** | Дешёвый, удобный интерфейс, бесплатный WHOIS privacy | Иногда медленная поддержка | от $8/год (.com) |
| **[Cloudflare Registrar](https://www.cloudflare.com/products/registrar/)** | По себестоимости, бесплатный CDN и DNS | Меньше зон, чем у конкурентов | от $8/год (.com) |
| **[Porkbun](https://porkbun.com)** | Очень дешёвый, бесплатный WHOIS privacy | Менее известный | от $7/год (.com) |
| **[reg.ru](https://reg.ru)** | Русскоязычный, оплата картой РФ | Дороже западных | от 800₽/год (.ru) |
| **[timeweb](https://timeweb.com/ru/services/domains/)** | Русскоязычный, простой интерфейс | Ограниченные зоны | от 199₽/год (.ru) |

### 📝 Пошаговая инструкция (Namecheap)

1. Перейдите на [namecheap.com](https://namecheap.com)
2. В поиске введите желаемое имя домена (например, `blinkmind.app`)
3. Выберите понравившуюся зону (.com, .app, .io и т.д.)
4. Нажмите **Add to cart** → **Checkout**
5. Создайте аккаунт (если нет)
6. Оплатите картой/PayPal/криптой
7. **Готово!** Домен ваш

### 💡 Советы по выбору домена

- `.com` — самый узнаваемый, но часто занят
- `.app` — отлично для приложений (требует HTTPS)
- `.io` — популярен среди стартапов
- `.ru` — если целевая аудитория в России

---

## 2. ⚡ Деплой фронтенда на Vercel

**Vercel** — лучший выбор для React/Vite проектов. Бесплатный план включает:
- 100 GB bandwidth/месяц
- Автоматический HTTPS
- Автоматический деплой из GitHub
- Preview для каждого PR

### 📝 Пошаговая инструкция

#### Шаг 1: Загрузите код на GitHub

```bash
# Если ещё не создали репозиторий
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на github.com, затем:
git remote add origin https://github.com/ВАШ_USERNAME/BlinkSite.git
git branch -M main
git push -u origin main
```

#### Шаг 2: Подключите Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **Sign Up** → войдите через **GitHub**
3. Нажмите **Add New** → **Project**
4. Найдите репозиторий `BlinkSite` → нажмите **Import**
5. Настройки оставьте по умолчанию (Vercel сам определит Vite)
6. Нажмите **Deploy**

#### Шаг 3: Подождите ~2 минуты

После деплоя вы получите URL вида:
```
https://blinksite-abc123.vercel.app
```

**🎉 Фронтенд задеплоен!**

---

## 3. 🚂 Деплой бэкенда на Railway

**Railway** — простой способ захостить Node.js сервер. Бесплатный план:
- $5 кредитов/месяц (достаточно для небольшого сервера)
- 512 MB RAM
- Автоматический деплой из GitHub

### Альтернативы Railway

| Сервис | Бесплатный план | Особенности |
|--------|-----------------|-------------|
| **[Railway](https://railway.app)** | $5/месяц кредитов | Простой, быстрый |
| **[Render](https://render.com)** | 750 часов/месяц | Засыпает при неактивности |
| **[Fly.io](https://fly.io)** | 3 маленьких VM бесплатно | Более сложная настройка |
| **[Cyclic](https://cyclic.sh)** | 100k запросов/месяц | Serverless |

### 📝 Пошаговая инструкция (Railway)

#### Шаг 1: Подготовьте бэкенд

Создайте файл `server/package.json`:

```json
{
  "name": "blinksite-server",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
```

#### Шаг 2: Зарегистрируйтесь на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите **Login** → войдите через **GitHub**
3. Нажмите **New Project** → **Deploy from GitHub repo**
4. Выберите репозиторий `BlinkSite`

#### Шаг 3: Настройте деплой

1. После импорта нажмите на созданный сервис
2. Перейдите в **Settings**
3. В разделе **Build** установите:
   - **Root Directory:** `server`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`

#### Шаг 4: Добавьте переменные окружения

1. Перейдите во вкладку **Variables**
2. Добавьте переменные:
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен_бота
   TELEGRAM_CHAT_ID=ваш_chat_id
   PORT=3001
   ```

#### Шаг 5: Получите URL

1. Перейдите в **Settings** → **Networking**
2. Нажмите **Generate Domain**
3. Вы получите URL вида:
   ```
   https://blinksite-server-production.up.railway.app
   ```

**🎉 Бэкенд задеплоен!**

---

## 4. 🔗 Подключение домена

### Подключение домена к Vercel (фронтенд)

#### На Vercel:

1. Откройте проект в Vercel
2. Перейдите в **Settings** → **Domains**
3. Введите ваш домен (например, `blinkmind.app`)
4. Vercel покажет DNS записи, которые нужно добавить

#### На Namecheap (или вашем регистраторе):

1. Войдите в аккаунт → **Domain List** → нажмите **Manage**
2. Перейдите в **Advanced DNS**
3. Добавьте записи, которые показал Vercel:

   | Type | Host | Value | TTL |
   |------|------|-------|-----|
   | A | @ | 76.76.21.21 | Auto |
   | CNAME | www | cname.vercel-dns.com | Auto |

4. Подождите 5-30 минут для применения DNS

### Подключение поддомена к Railway (бэкенд)

Если хотите красивый URL для API (например, `api.blinkmind.app`):

#### На Railway:

1. **Settings** → **Networking** → **Custom Domain**
2. Введите `api.blinkmind.app`
3. Railway покажет CNAME запись

#### На Namecheap:

1. Добавьте CNAME запись:

   | Type | Host | Value | TTL |
   |------|------|-------|-----|
   | CNAME | api | your-app.up.railway.app | Auto |

---

## 5. ⚙️ Финальная настройка

### Обновите URL API во фронтенде

После деплоя бэкенда нужно обновить URL в коде фронтенда.

Создайте файл `.env.production` в корне проекта:

```env
VITE_API_URL=https://api.blinkmind.app
```

Или если без кастомного домена:
```env
VITE_API_URL=https://blinksite-server-production.up.railway.app
```

### Обновите код App.tsx

Найдите место, где отправляется форма, и используйте переменную окружения:

```typescript
const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3001'

// В функции отправки формы:
const response = await fetch(`${API_URL}/api/send-form`, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify(formData)
})
```

### Обновите CORS на бэкенде

В `server/index.js` обновите настройки CORS:

```javascript
// Было:
app.use(cors())

// Стало:
app.use(cors({
  origin: [
    'http://localhost:5173',           // Локальная разработка
    'https://blinkmind.app',           // Продакшн
    'https://www.blinkmind.app',       // С www
    'https://blinksite.vercel.app'     // Vercel превью
  ]
}))
```

### Добавьте переменные в Vercel

1. Откройте проект в Vercel
2. **Settings** → **Environment Variables**
3. Добавьте:
   ```
   VITE_API_URL=https://api.blinkmind.app
   ```
4. Нажмите **Redeploy** для применения

---

## 📊 Итоговая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                         ИНТЕРНЕТ                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    blinkmind.app (DNS)                       │
│                                                              │
│   ┌─────────────────┐          ┌─────────────────┐          │
│   │  blinkmind.app  │          │ api.blinkmind.app│          │
│   │   (Vercel)      │  ──────► │    (Railway)     │          │
│   │   Фронтенд      │  API     │    Бэкенд        │          │
│   └─────────────────┘          └─────────────────┘          │
│          │                              │                    │
└──────────┼──────────────────────────────┼────────────────────┘
           │                              │
           ▼                              ▼
    ┌─────────────┐               ┌─────────────┐
    │   GitHub    │               │  Telegram   │
    │ (репозит.)  │               │     API     │
    └─────────────┘               └─────────────┘
```

---

## 💰 Стоимость

| Компонент | Сервис | Цена |
|-----------|--------|------|
| Домен | Namecheap | ~$10/год |
| Фронтенд | Vercel | **Бесплатно** |
| Бэкенд | Railway | **Бесплатно** (до $5/месяц) |
| **Итого** | | **~$10/год** |

---

## 🔧 Чеклист перед запуском

- [ ] Домен куплен
- [ ] GitHub репозиторий создан
- [ ] Фронтенд задеплоен на Vercel
- [ ] Бэкенд задеплоен на Railway
- [ ] Переменные окружения настроены (TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID)
- [ ] DNS записи настроены
- [ ] CORS обновлен для продакшн домена
- [ ] API_URL обновлен во фронтенде
- [ ] Форма работает и отправляет сообщения в Telegram

---

## ❓ Частые проблемы

### CORS ошибка
**Проблема:** `Access to fetch at... has been blocked by CORS policy`

**Решение:** Добавьте ваш домен в настройки CORS на бэкенде.

### DNS не работает
**Проблема:** Сайт не открывается по домену

**Решение:** Подождите до 48 часов (обычно 5-30 минут). Проверьте DNS на [dnschecker.org](https://dnschecker.org)

### Railway засыпает
**Проблема:** Первый запрос занимает долго

**Решение:** Это нормально для бесплатного плана. Или перейдите на платный план ($5/месяц).

### Переменные окружения не работают
**Проблема:** Telegram бот не отправляет сообщения

**Решение:** Проверьте, что переменные добавлены в Railway и сервис перезапущен.

---

## 🎓 Полезные ссылки

- [Документация Vercel](https://vercel.com/docs)
- [Документация Railway](https://docs.railway.app/)
- [Проверка DNS](https://dnschecker.org/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

**Удачного деплоя! 🚀**
